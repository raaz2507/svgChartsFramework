# svgChartsFramework
A lightweight and customizable SVG-based chart framework built with pure JavaScript. Supports dynamic data rendering, animations, and interactive tooltips without external libraries.
